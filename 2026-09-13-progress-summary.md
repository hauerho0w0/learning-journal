# 研究進度摘要：以向量圖作為訓練輸入的標註效率

- 日期：2026-09-11（v2）
- 資料集：SESYD Floorplans（1000 張、28,065 個物件、16 類）
- 基準模型：YOLaT（NeurIPS 2021）
- 硬體：RTX 4060 Laptop 8GB

---

## 一、研究問題

向量圖與點陣圖描述同一張圖，但向量格式額外免費提供三種結構資訊：線條之間的**連通性**、
同一路徑或圖層的**群組關係**、以及線寬顏色等**離散屬性**。點陣模型必須從像素把這些重新學出來。

本研究要問的是：這些先驗能不能換成「更少的人工標註」。

| 假設 | 內容 | 現況 |
|---|---|---|
| **H1（效果）** | 同任務同資料下，向量輸入不劣於點陣輸入 | 文獻已有支持（YOLaT、SymPoint），非本研究主要貢獻 |
| **H2（標註效率，主假設）** | 達到同等效果，向量輸入所需標註量顯著較少 | **文獻檢索確認：目前沒有任何論文做過同任務同資料的向量 vs 點陣標註量對照實驗。這是本研究要填補的缺口。** |

適用範圍限定在**原生向量資料**（CAD 圖、電路圖、圖表），不主張「把照片向量化會更好」。

---

## 二、目前進度

| 階段 | 內容 | 狀態 |
|---|---|---|
| R1-1 | 文獻驗證與論點修正 | ✅ 完成 |
| R1-2 | 實作 YOLaT 模型與完整實驗管線（SVG 解析、建圖、proposal、雙流 GNN、評估指標，全部自建） | ✅ 完成 |
| R1-3 | 重現原論文結果 | ✅ 完成（已釐清無法完全重現的三項具體原因） |
| R1-4 | 向量組標註效率曲線（8 個標註比例 × 5 個隨機種子 = 40 次訓練） | ✅ 完成 |
| R1-5 | 點陣對照組與公平性修正 | 🔵 進行中 |
| R2 | 模型內層改造（移植 SymPoint 的連接注意力與對比損失） | ⬜ 待辦 |

---

## 三、四項關鍵發現

前三項是重現過程中發現的問題，第四項是修正自己實驗設計缺陷後得到的結果。

### 發現一：SESYD 的 SVG 只含符號，牆體其實是點陣背景層

1000 張圖**全部**都是「一張 PNG 背景（牆與房間）＋ 向量圖層（家具符號）」的結構。

因此向量模型看到的是**已經做過背景減除的乾淨版本**，而點陣模型必須在牆體雜訊中找符號。
實測背景多帶進 **3.4 倍**的筆畫量。原論文的「向量勝點陣」有一部分來自這個不對等。

### 發現二：1000 張圖只有 10 種版面，隨機切分會造成資料洩漏

10 個子資料夾各共用一張背景，即「10 種版面 × 100 種家具擺放」。
隨機切分會讓同一版面同時出現在訓練集與測試集。

改用「依版面分組」的無洩漏切分後，同一套程式的 AP50 **從 97.3 掉到 14.7**。
模型原本是靠記憶「家具在版面上的固定位置」作答；改用相對座標後回升到 **59.4**，
但與洩漏切分仍有 **28 分**差距。

> **意涵**：原論文回報的數字建立在共用版面的隨機切分上，可能高估了跨版面的泛化能力。
> 此點可獨立成文。

### 發現三：預測框少算半個線寬，使重現在幾何上就不可能達標

標準答案的框涵蓋**畫出來的墨跡**，我原本的框用的是**幾何中心線**，每邊少了半個線寬。

差這一點，就讓 mAP 的理論上限只有 **84.7%**——低於論文的 90.6%。
也就是說，修正前無論分類器多完美都追不到。修正後上限升到 95.2%。

> **方法上的收穫**：先計算「上限」再調參，能證明某條路走不通，
> 而不是一直以為「只是還沒調好」。

### 發現四：資料增強的不對等，一度讓結論完全反過來

第一版對照實驗中，點陣組開著 YOLO 預設的完整資料增強，向量組完全沒有。
在只有 2 張訓練圖時，這是決定性的差異——第一個結果顯示「點陣組贏 7 倍」。

補上**向量專屬的精確增強**後重做成 2×2 因子實驗（同一個 cell、相同的 2 張圖、相同訓練預算）：

| | 無增強 | 有增強 | 增強帶來的增益 |
|---|---|---|---|
| **向量 GNN**（367 k 參數） | **4.62** | 29.45 | 6.4× |
| **點陣 YOLOv8n**（3.2 M 參數） | **0.29** | 34.00 | **119×** |
| **向量 / 點陣** | **16.2×** | 0.87× | |

**在完全對稱的無增強條件下，向量組贏 16 倍。**
點陣組 mAP 只有 0.29，等於什麼都沒學到——隨機初始化的 CNN 在 2 張圖上本來就學不起來，
而向量模型靠結構先驗還能做到 4.62。**這正是 H2 主張的機制。**

先前「點陣贏 7 倍」的結論，完全是實驗設計缺陷的產物。

#### 但另一半也要誠實看待

兩邊都用上各自自然的增強後，差距就消失了（29.45 vs 34.00，點陣略勝 15%）。
而且**增強對點陣組的幫助是 119 倍，對向量組只有 6.4 倍**。

> 向量的結構先驗與資料增強，**在功能上是重疊的**——兩者都在告訴模型
> 「哪些變化不改變語意」。向量格式把這件事內建在表示裡，點陣模型必須靠增強從外部灌進去。
> 一旦強增強到位，先驗的邊際價值就大幅下降。

**因此 H2 的表述修正為**：

> 向量的結構先驗，在**缺乏或無法使用強資料增強**的條件下提供顯著的標註效率優勢；
> 雙方皆使用其自然增強時，優勢大幅縮小。

這比原本的無條件主張更精確，也更容易站得住腳。

#### 附帶收穫：向量增強是「無損」的

旋轉、翻轉、縮放**直接作用在控制點上，是解析且無誤差的**，
不像點陣圖會產生插值模糊（此資料集最細的線只有 0.89 像素寬）。

而且因為模型結構上是 cluster-local 的（跨符號沒有任何資訊流），
**每個符號可以獨立變換**——2 張圖就能產生 4²⁸ 種組合。

正確性已驗證：不變式在增強前後誤差 1.6e-7（浮點精度極限），標籤零變動。

---

## 四、目前數據

### 向量組標註效率曲線

無洩漏的分組切分，每點 5 個隨機種子。

| 標註比例 | 訓練圖數 | mAP | ± 標準差 | 佔滿標註 |
|---|---|---|---|---|
| 0.5% | 2 | 4.91 | 1.61 | 9.5% |
| 1% | 5 | 21.13 | 7.78 | 41.0% |
| 2% | 10 | 40.59 | 6.09 | 78.7% |
| **5%** | **25** | **46.77** | **2.91** | **90.7%** |
| 10% | 50 | 51.28 | 3.18 | 99.4% |
| 25% | 125 | 54.08 | 3.47 | 104.9% |
| 50% | 250 | 55.00 | 3.16 | 106.7% |
| 100% | 500 | 51.56 | 2.58 | 100.0% |

**25 張圖就達到滿標註的 90.7%。** 曲線在 5% 之後趨於平坦，
代表這個資料集的**可辨別區間只有最左側三、四個點**；
右側四點在誤差範圍內重疊，對 H2 沒有鑑別力。

### 向量組加上精確增強後（分組切分，各 5 seeds）

| 標註比例 | 訓練圖數 | 無增強 | 有增強 | 增益 |
|---|---|---|---|---|
| 0.5% | 2 | 4.91 ± 1.61 | **31.39 ± 6.95** | **6.4×** |

1%、2%、5% 三組執行中。

完整的四方對照見**發現四**。向量組的參數量只有點陣組的 **1/9**，
且尚無框迴歸機制——這兩項因素目前仍偏向點陣組。

---

## 五、下一步

1. **完成向量組的增強版曲線**
   0.5% / 1% / 2% / 5% 四個比例 × 5 seeds，與既有的無增強曲線直接對照。
   *執行中，約 6 小時。*

2. **補跑「雙方皆無增強」的完整點陣曲線**
   目前無增強對照只有 0.5% 一個點。**H2 最強的證據形式是「雙方皆無增強」的完整曲線對比**，
   需補跑 0.5/1/2/5% × 5 seeds。

3. **完成有增強的點陣對照組曲線**
   YOLOv8n，隨機初始化不載入預訓練權重，與向量組共用同一批圖片與訓練預算；
   同時跑「含牆體」與「僅向量層」兩種渲染，量化發現一的偏差大小。
   *約 39 小時（本機）。*

4. **對 H2 給出判定**
   比較兩條曲線在低標註區間的**衰減斜率**，而非單點數值，並分別在「有增強」與「無增強」
   兩種條件下報告。

5. **視結果決定是否更換資料集**
   SESYD 飽和太早。若鑑別力不足，改用 FloorPlanCAD（11,602 張真實 CAD 圖、35 類），
   多樣性高一個數量級。
   *需新解析器，約 1–2 天。*

---

## 六、已知限制

- **重現未完全達標。** 修正三項問題後 mAP 從 11.7 推到 75.7，論文為 90.6。
  剩餘差距已定位為「缺乏定位品質訊號」，屬架構層新增，暫不在本輪處理。
- **資料集天花板。** SESYD 為合成資料，僅 10 種版面、模板化符號，曲線右側無鑑別力。
- **光柵化解析度。** 本資料集最細筆畫在 1024 像素渲染下僅 0.89 像素寬，屬次像素。
  若點陣組表現異常低落，此為第一順位待查變因。
- **結論範圍。** 所有結論僅適用於原生向量資料。

---

*程式碼與完整實驗紀錄：`src/yolat/`、`scripts/`、`docs/research-plan.md`（v2.1，含全部決策與變更歷程）*
